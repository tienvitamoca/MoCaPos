version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm install -g @oracle/ojet-cli
    build:
      commands:
        - ojet build --release
  artifacts:
    baseDirectory: web
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
